# Правила хуков и custom hooks

<!-- CARD-NAV-TOP:START -->
[← 07 useEffect useLayoutEffect и cleanup](<./07 useEffect useLayoutEffect и cleanup.md>) · [↑ React](<./README.md>) · [⌂ Все разделы](<../../README.md>) · [09 useMemo useCallback и React memo →](<./09 useMemo useCallback и React memo.md>)
<!-- CARD-NAV-TOP:END -->

## Вопрос

<br>

 💬 **Какие правила действуют для хуков React? Для чего создают пользовательские хуки?**

<h2></h2>

<br>
<dl>
<dd>

Хуки (hooks) являются функциями React, которые подключают компонент к состоянию, Context, `ref`, эффектам и другим возможностям. Обычные хуки нужно вызывать только на верхнем уровне функционального компонента или пользовательского хука, в одинаковом порядке при каждом рендере. Их нельзя помещать в условие, цикл, вложенную функцию, обработчик события, `try/catch` или код после условного `return`.

React связывает состояние хука с позицией его вызова в конкретном компоненте. Если сейчас первым вызван `useState`, а в следующем рендере условие пропустило его, второй хук займёт прежнюю позицию и получит чужое состояние. Поэтому условие помещают внутрь хука или разбивают интерфейс на отдельные компоненты:

```tsx
useEffect(() => {
  if (!isConnected) return;
  return subscribe();
}, [isConnected]);
```

Второе правило: хуки вызывают только во время рендера React-компонента или другого пользовательского хука. Обычная вспомогательная функция может быть вызвана вне React и не имеет жизненного цикла хука. Имя пользовательского хука начинается с `use`, чтобы линтер и разработчик распознавали функцию, которая подчиняется этим правилам.

API `use` из React 19 является специальным исключением: `use(promise)` и `use(context)` можно вызывать в условиях и циклах. При этом `use` всё равно разрешён только внутри компонента или пользовательского хука, и его нельзя оборачивать в `try/catch`. Остальные хуки, включая `useState` и `useEffect`, сохраняют обычное требование неизменного порядка.

Пользовательский хук переиспользует логику состояния и эффектов, но не JSX. Например, `useOnlineStatus` скрывает детали подписки на состояние сети, а `useDebouncedValue` связывает состояние и таймер. Каждый вызов пользовательского хука получает собственные состояние и эффекты. Общее состояние появится только тогда, когда реализация подписывается на общий Context, внешнее хранилище или кеш.

Хороший пользовательский хук выражает понятный сценарий и скрывает детали его синхронизации. Он не должен быть механической оболочкой над одним `useState` без добавленного смысла. Его входы и возвращаемые значения являются публичным контрактом: новые ссылки на функции или объекты могут заставить потребителя лишний раз запускать эффекты, поэтому стабильность ссылок выбирают осознанно.

`eslint-plugin-react-hooks` проверяет порядок вызовов, зависимости эффектов и дополнительные правила React. В актуальных версиях он также содержит диагностику, связанную с React Compiler. Отключать правило `exhaustive-deps` вместо исправления эффекта опасно: в коде может остаться устаревшее замыкание, которое трудно заметить при обычном тестировании.

</dd>
</dl>
<br>


## Дополнительные вопросы

<details>
<summary><strong>Почему <code>useEffect</code> нельзя вызвать внутри <code>if</code>?</strong></summary>

<dl>
<dd>
<h2></h2>

Условие меняет число и порядок обычных хуков между рендерами. React сопоставляет их состояние по позиции вызова, поэтому следующий хук может получить данные предыдущего. Нужно вызвать хук безусловно, а проверку поместить внутрь его функции.

<h2></h2>
</dd>
</dl>

</details>

<details>
<summary><strong>Почему <code>use</code> можно вызывать условно?</strong></summary>

<dl>
<dd>
<h2></h2>

`use` в React 19 имеет отдельную реализацию и не хранит локальное состояние по обычной позиции вызова хука. Поэтому его разрешено использовать в условиях и циклах. Это исключение не распространяется на `useState`, `useEffect` и другие хуки; `use` также нельзя вызывать вне компонента или оборачивать в `try/catch`.

<h2></h2>
</dd>
</dl>

</details>

<details>
<summary><strong>Делят ли два компонента состояние одного пользовательского хука?</strong></summary>

<dl>
<dd>
<h2></h2>

Нет. Пользовательский хук переиспользует код, а каждый вызов создаёт собственную цепочку хуков в компоненте. Для общего состояния хук должен обратиться к общему источнику: Context, внешнему хранилищу, намеренно общему объекту модуля или кешу запросов.

<h2></h2>
</dd>
</dl>

</details>

<details>
<summary><strong>Может ли пользовательский хук возвращать JSX?</strong></summary>

<dl>
<dd>
<h2></h2>

Технически функция способна вернуть любое значение, но хук предназначен для переиспользования логики, а компонент для описания интерфейса. Если основным результатом является JSX, яснее создать компонент. Хук может вернуть состояние, данные, функции и `props`, необходимые компоненту без собственной разметки.

<h2></h2>
</dd>
</dl>

</details>

<details>
<summary><strong>Зачем нужен <code>eslint-plugin-react-hooks</code>?</strong></summary>

<dl>
<dd>
<h2></h2>

Нарушение порядка и пропущенная зависимость могут проявиться только в определённой последовательности рендеров. Статический анализ находит эти ошибки раньше. Актуальная версия плагина также проверяет дополнительные правила чистоты и совместимость кода с React Compiler.

<h2></h2>
</dd>
</dl>

</details>

<details>
<summary><strong>Всегда ли повторяющуюся логику нужно выносить в пользовательский хук?</strong></summary>

<dl>
<dd>
<h2></h2>

Нет. Два похожих фрагмента могут иметь разные причины изменения и разойтись в будущем. Хук оправдан, когда существует единая абстракция с понятным названием, жизненным циклом и контрактом. Слишком общий `useSomething` с множеством флагов лишь прячет сложность.

<h2></h2>
</dd>
</dl>

</details>

## Где это встречается во frontend

| Ситуация | Возможный пользовательский хук |
| --- | --- |
| Подписка на состояние сети | `useOnlineStatus` |
| Наблюдение за CSS-медиазапросом | `useMediaQuery` |
| Задержка поискового значения | `useDebouncedValue` |
| Интеграция `ResizeObserver` | `useResizeObserver` |
| Общий сценарий запроса | Хук поверх RTK Query с предметными аргументами |
| Компонент без собственной разметки | Хук возвращает состояние, `props` и обработчики |

## Связанные темы

- [07 useEffect useLayoutEffect и cleanup](<./07 useEffect useLayoutEffect и cleanup.md>)
- [20 React Compiler](<./20 React Compiler.md>)
- [25 Advanced hooks useId useSyncExternalStore useOptimistic use](<./25 Advanced hooks useId useSyncExternalStore useOptimistic use.md>)
- [08 Замыкание](<../JavaScript/08 Замыкание.md>)

## Источники

- [React: Rules of Hooks](https://react.dev/reference/rules/rules-of-hooks)
- [React: Reusing Logic with Custom Hooks](https://react.dev/learn/reusing-logic-with-custom-hooks)
- [React: `eslint-plugin-react-hooks`](https://react.dev/reference/eslint-plugin-react-hooks)
- [React: `use`](https://react.dev/reference/react/use)

---

<!-- CARD-NAV-BOTTOM:START -->
[← 07 useEffect useLayoutEffect и cleanup](<./07 useEffect useLayoutEffect и cleanup.md>) · [↑ React](<./README.md>) · [⌂ Все разделы](<../../README.md>) · [09 useMemo useCallback и React memo →](<./09 useMemo useCallback и React memo.md>)
<!-- CARD-NAV-BOTTOM:END -->
