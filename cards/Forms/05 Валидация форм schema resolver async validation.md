# 05 Валидация форм schema resolver async validation

<!-- CARD-NAV-TOP:START -->
[← 04 Controller и кастомные компоненты](<./04 Controller и кастомные компоненты.md>) · [↑ Forms](<./README.md>) · [⌂ Все разделы](<../../README.md>) · [06 Submit lifecycle server errors reset defaultValues →](<./06 Submit lifecycle server errors reset defaultValues.md>)
<!-- CARD-NAV-TOP:END -->

## Вопрос

Как организовать валидацию формы? Чем отличаются правила поля, schema resolver и асинхронная валидация?

<details>
<summary><strong>Показать ответ</strong></summary>

Проверка формы строится слоями. Нативные ограничения HTML (`required`, `min`, `pattern`, `type="email"`) дают базовую проверку браузера. Правила поля (`field-level rules`) проверяют одно значение. Межполевая валидация (`cross-field validation`) проверяет связь нескольких значений, например совпадение паролей или порядок дат. Сервер окончательно проверяет права, уникальность, бизнес-правила и актуальное состояние данных.

Схемная валидация (`schema validation`) описывает форму целиком через Zod, Yup, Ajv или другую библиотеку. `Resolver` - адаптер между схемой и React Hook Form: он получает значения и возвращает объекты `values` и `errors` в формате RHF. Многие resolvers умеют вывести TypeScript-тип из схемы. Если схема преобразует данные, входной и выходной типы могут различаться; это отражают в generic-параметрах `useForm` и в DTO, то есть в типе объекта запроса к API.

В RHF resolver становится единым механизмом клиентской проверки и не используется одновременно со встроенными правилами `register`, такими как `required` или `min`. Простое ограничение в таком случае переносят в схему, чтобы не получить два несогласованных источника ошибок.

Асинхронная валидация нужна, когда результат зависит от внешнего ресурса: занят ли email, действует ли промокод. Запрос не отправляют на каждый символ: выбирают момент потери фокуса (`blur`) или ждут паузу во вводе с помощью `debounce`. Устаревший запрос отменяют через `AbortController` либо сверяют идентификатор запроса и текущее значение. У поля показывают состояние проверки, но окончательная отправка всё равно повторяет проверку на сервере.

Момент показа зависит от сценария. `mode: 'onSubmit'` не отвлекает до первой отправки, `onBlur` проверяет после выхода из поля, `onChange` даёт раннюю обратную связь, но может запускать проверку всей схемы на каждый символ. После первой неудачной отправки `reValidateMode` определяет, когда проверять исправления.

Сервер возвращает стабильные коды ошибок и пути полей, а frontend сопоставляет их с именами формы и локализованным текстом. Ошибку поля задают через `setError('email', ...)`, общую - через `setError('root.serverError', ...)` или отдельное состояние операции. Введённые значения при ошибке сохраняют, а сообщение связывают с полем и объявляют доступным способом.

</details>

## Встречные вопросы

<details>
<summary><strong>Вопрос:</strong> Что такое resolver в React Hook Form?</summary>

Resolver - адаптер между RHF и библиотекой схемной валидации. Он получает значения, объект дополнительных данных `context` и сведения о проверяемых полях, запускает схему и возвращает объект с `values` и `errors`. Ошибки должны иметь вложенную структуру, совпадающую с путями полей, например `participants[1].name`, а не только плоский строковый ключ.

Готовые resolvers для Zod, Yup и других библиотек выполняют это преобразование и часто выводят выходной TypeScript-тип из схемы.

</details>

<details>
<summary><strong>Вопрос:</strong> Можно ли одновременно использовать resolver и rules из <code>register</code>?</summary>

В React Hook Form resolver не комбинируется со встроенными правилами валидации вроде `required` и `min`. Если форма использует схему, эти правила описывают в ней, а `register` оставляют для подключения поля и преобразования вроде `valueAsNumber`. Так у формы остаётся один источник клиентских ошибок.

</details>

<details>
<summary><strong>Вопрос:</strong> Когда использовать нативную валидацию, а когда <code>noValidate</code>?</summary>

Нативные `required`, `type`, `min` и другие ограничения дают браузерную проверку без библиотеки. Если приложение хочет полностью управлять моментом и видом сообщений через RHF и схему, форме ставят `noValidate`. Тогда браузер не остановит отправку собственным всплывающим сообщением до вызова обработчика. Семантические атрибуты и доступные тексты ошибок при этом всё равно сохраняют.

</details>

<details>
<summary><strong>Вопрос:</strong> Где проверять, что пароль и его подтверждение совпадают?</summary>

Это межполевое правило, потому что результат зависит от двух значений. В схеме проверяют объект целиком и привязывают ошибку к `confirmPassword`. Если использовать функцию `validate`, она должна получать остальные значения формы, а изменение исходного `password` должно повторно запустить проверку подтверждения.

</details>

<details>
<summary><strong>Вопрос:</strong> Что такое race condition при асинхронной валидации?</summary>

Пользователь быстро меняет значение, и старый запрос может вернуться позже нового. Если без проверки применить старый результат, форма покажет ошибку не для текущего значения.

Пример: пользователь ввёл `alex`, затем быстро заменил его на `alex2`. Ответ для `alex` может прийти последним и не должен установить ошибку текущему значению. Используют `AbortController`, последовательный идентификатор запроса или сравнение проверенного значения с актуальным. `Debounce`, то есть ожидание паузы во вводе, уменьшает число запросов, но сам по себе не гарантирует правильный порядок ответов.

</details>

<details>
<summary><strong>Вопрос:</strong> Если есть клиентская валидация, зачем ещё серверная?</summary>

Клиентская проверка быстрее подсказывает ошибку и не отправляет очевидно неверные данные. Но код браузера находится под контролем пользователя и не является доверенной средой.

Сервер повторно проверяет данные, права, бизнес-правила, уникальность, лимиты и состояние системы. Email мог быть свободен во время предварительной проверки, но занят другим запросом к моменту отправки формы.

</details>

<details>
<summary><strong>Вопрос:</strong> Как показать ошибку поля, которую вернул backend?</summary>

Слой адаптации сопоставляет путь и код из ответа с именем и локализованным текстом формы, затем вызывает `setError("email", { type: "server", message })`. Для общей ошибки используют `root.serverError` или отдельное состояние запроса.

Сервер может вернуть `field: "user.email"`, а форма ожидать `email`. Если это сопоставление размазано по компонентам, изменение API станет дорогим и ошибки будут отображаться непоследовательно.

</details>

<details>
<summary><strong>Вопрос:</strong> Как выбрать <code>mode</code> в React Hook Form?</summary>

`onSubmit` подходит, когда не нужно показывать ошибки до первой попытки. `onBlur` даёт обратную связь после завершения ввода в поле. `onChange` нужен для требований, которые полезно подтверждать сразу, но увеличивает число рендеров и запусков схемы. Часто выбирают `onSubmit` или `onBlur`, а после ошибки повторно проверяют изменения через `reValidateMode: 'onChange'`.

</details>

## Где это встречается во frontend

> [!NOTE]
> | Проверка | Где делать |
> | --- | --- |
> | `required`/`minLength` | Нативные правила или схема |
> | Подтверждение пароля | Межполевая проверка в схеме |
> | Доступность имени пользователя | Асинхронная проверка с отменой устаревших запросов |
> | Бизнес-правило | Обязательная проверка на сервере и ранняя подсказка во frontend |
> | Ошибки полей API | Адаптер путей и `setError` |

## Связанные темы

- [03 React Hook Form register handleSubmit formState](<./03 React Hook Form register handleSubmit formState.md>)
- [06 Submit lifecycle server errors reset defaultValues](<./06 Submit lifecycle server errors reset defaultValues.md>)
- [08 Accessibility форм](<./08 Accessibility форм.md>)
- [18 Проверка данных с backend](<../TypeScript/18 Проверка данных с backend.md>)

## Источники

- [React Hook Form README](https://github.com/react-hook-form/react-hook-form)
- [React Hook Form docs: resolver](https://react-hook-form.com/docs/useform#resolver)
- [React Hook Form Resolvers](https://github.com/react-hook-form/resolvers)
- [MDN: Client-side form validation](https://developer.mozilla.org/en-US/docs/Learn_web_development/Extensions/Forms/Form_validation)

---

<!-- CARD-NAV-BOTTOM:START -->
[← 04 Controller и кастомные компоненты](<./04 Controller и кастомные компоненты.md>) · [↑ Forms](<./README.md>) · [⌂ Все разделы](<../../README.md>) · [06 Submit lifecycle server errors reset defaultValues →](<./06 Submit lifecycle server errors reset defaultValues.md>)
<!-- CARD-NAV-BOTTOM:END -->
