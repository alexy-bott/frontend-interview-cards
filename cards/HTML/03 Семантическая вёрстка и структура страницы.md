# Семантическая вёрстка и структура страницы

<!-- CARD-NAV-TOP:START -->
[← 02 Блочные и строчные элементы div и span](<./02 Блочные и строчные элементы div и span.md>) · [↑ HTML](<./README.md>) · [⌂ Все разделы](<../../README.md>) · [04 Основы доступности в HTML →](<./04 Основы доступности в HTML.md>)
<!-- CARD-NAV-TOP:END -->

## Вопрос

<br>

 💬 **Что такое семантическая верстка? Зачем нужны смысловые области страницы и правильная структура заголовков?**

<h2></h2>

<br>
<dl>
<dd>

Семантическая вёрстка — использование HTML-элементов в соответствии с назначением содержимого и действия. Например, `nav` обозначает навигацию, `main` — основное содержимое документа, `article` — самостоятельный материал, `button` — действие, а `a` с `href` — переход по адресу. CSS может изменить внешний вид, но не назначение этих элементов.

Landmarks, или ориентиры страницы, — крупные области, между которыми пользователь вспомогательной технологии может быстро перемещаться. `main`, `nav`, `aside` и `<search>` создают соответствующие landmarks. `form` и `section` становятся landmarks, когда у них есть доступное имя.

`header` соответствует landmark `banner`, а `footer` — `contentinfo`, когда они относятся к документу в целом. Если `header` или `footer` находятся внутри `article`, `aside`, `main`, `nav` или `section`, они описывают эту локальную область и не становятся `banner` или `contentinfo`.

В документе не должно быть больше одного `<main>` без атрибута `hidden`. Он представляет доминирующее содержимое текущего документа.

Если landmark одного типа встречается несколько раз, его экземпляры нужно различать доступными именами, когда у них разное назначение. Например, две разные области `nav` можно назвать «Основная» и «По разделу». Если две области навигации содержат один и тот же набор ссылок и выполняют одну функцию, одинаковое имя допустимо.

Заголовки `h1`–`h6` задают иерархию содержимого. Уровень выбирают по месту раздела в структуре документа, а не по желаемому размеру шрифта: `h1` — заголовок верхнего уровня, `h2` — следующего, `h3` — вложенного в него и так далее. Визуальный размер задаёт CSS.

Не следует рассчитывать, что `section` или `article` автоматически исправят уровень вложенного заголовка. Нужный `h1`–`h6` выбирают явно по иерархии документа.

Семантические элементы напрямую сообщают браузеру и вспомогательным технологиям роль содержимого и связи между его частями. Для интерактивных элементов нативный HTML также даёт ожидаемое поведение, поэтому подходящий HTML-элемент предпочтительнее нейтрального контейнера с вручную добавленной ролью.

ARIA используют, когда нативной семантики недостаточно. Она может добавить или уточнить роль, имя, свойства и состояния в дереве доступности, но сама по себе не реализует клавиатурное или другое нативное поведение.

</dd>
</dl>
<br>


## Дополнительные вопросы

<details>
<summary><strong>Что плохого в верстке из одних <code>div</code>?</strong></summary>

<dl>
<dd>
<h2></h2>

`div` — нейтральный контейнер без собственной семантики. Если вся страница построена только из `div`, дерево элементов не сообщает, где находится навигация, основное содержимое, список или кнопка.

Для интерактивных `div` придётся вручную добавлять роль, фокус и клавиатурное поведение. При этом `div` остаётся правильным выбором, когда контейнер нужен только для группировки или раскладки и не имеет отдельного смысла.

<h2></h2>
</dd>
</dl>

</details>

<details>
<summary><strong>Когда использовать <code>section</code>?</strong></summary>

<dl>
<dd>
<h2></h2>

Когда содержимое образует тематический раздел документа, который имеет смысл обозначить в общей структуре и обычно можно озаглавить. Если контейнер нужен только для Grid, Flexbox или отступов, подходит `div`.

Сам по себе `<section>` без доступного имени не становится landmark `region`. Если раздел действительно нужен как отдельный ориентир, ему можно дать доступное имя, например через `aria-labelledby`, ссылающийся на его заголовок.

Не нужно заменять элементом `section` каждый `div` только ради «более семантической» разметки.

<h2></h2>
</dd>
</dl>

</details>

<details>
<summary><strong>Может ли быть несколько <code>h1</code>?</strong></summary>

<dl>
<dd>
<h2></h2>

Стандарт не запрещает несколько `h1`, но для обычной страницы один главный заголовок делает структуру предсказуемее для пользователей, вспомогательных технологий и разработчиков.

Не следует полагаться на то, что `h1` внутри каждого `section` автоматически станет заголовком нужного уровня. Уровни выбирают явно по иерархии документа и не используют `h3` вместо `h2` только из-за подходящего размера шрифта.

<h2></h2>
</dd>
</dl>

</details>

<details>
<summary><strong>ARIA может заменить неправильный HTML?</strong></summary>

<dl>
<dd>
<h2></h2>

ARIA может сообщить вспомогательной технологии роль или состояние, но не добавляет нативное поведение.

Например, `role="button"` на `div` не реализует активацию Enter и Space, фокус и настоящее отключённое состояние. Аналогично `aria-disabled="true"` сообщает об отключённости, но само по себе не предотвращает вызов обработчика.

Неверная ARIA способна сделать интерфейс менее понятным, скрыв правильную семантику или объявив поведение, которого компонент фактически не поддерживает.

<h2></h2>
</dd>
</dl>

</details>

<details>
<summary><strong>Когда использовать <code>&lt;button&gt;</code>, а когда <code>&lt;a&gt;</code>?</strong></summary>

<dl>
<dd>
<h2></h2>

`<button>` запускает действие в текущем интерфейсе: отправляет форму, открывает диалог, переключает режим или удаляет запись.

`<a href>` переводит пользователя к адресу или ресурсу. Ссылка поддерживает копирование URL, открытие в новой вкладке и контекстное меню, поэтому подменять её кнопкой в SPA не следует. Компонент роутера для навигации в итоге должен рендерить ссылку.

Внешний вид не определяет выбор: ссылку можно оформить как большую кнопку, а кнопку — как текст. Решение принимают по результату взаимодействия: переход по URL или действие.

<h2></h2>
</dd>
</dl>

</details>

## Где это встречается во frontend

| UI | Семантика |
| --- | --- |
| Главное содержимое | `main` |
| Меню | `nav` |
| Карточка статьи | `article` |
| Кнопка сохранения | `button` |
| Ссылка на страницу | `a href` |
| Заголовок блока | `h2`/`h3` по структуре |

## Связанные темы

- [02 Блочные и строчные элементы div и span](<./02 Блочные и строчные элементы div и span.md>)
- [04 Основы доступности в HTML](<./04 Основы доступности в HTML.md>)
- [02 Семантический HTML и ARIA](<../Accessibility/02 Семантический HTML и ARIA.md>)
- [05 Тестирование React с React Testing Library](<../Testing/05 Тестирование React с React Testing Library.md>)

## Источники

- [WHATWG HTML: Sections](https://html.spec.whatwg.org/multipage/sections.html)
- [WHATWG HTML: The main element](https://html.spec.whatwg.org/multipage/grouping-content.html#the-main-element)
- [WAI APG: Landmark Regions](https://www.w3.org/WAI/ARIA/apg/practices/landmark-regions/)
- [WAI: Page Structure](https://www.w3.org/WAI/tutorials/page-structure/)

---

<!-- CARD-NAV-BOTTOM:START -->
[← 02 Блочные и строчные элементы div и span](<./02 Блочные и строчные элементы div и span.md>) · [↑ HTML](<./README.md>) · [⌂ Все разделы](<../../README.md>) · [04 Основы доступности в HTML →](<./04 Основы доступности в HTML.md>)
<!-- CARD-NAV-BOTTOM:END -->
